# =============================================================================
# Prior-Auth Co-pilot — Python dependencies
# =============================================================================
#
# ONE-TIME SETUP (run these once after cloning):
#
#   1. Install deps:
#        pip install -r requirements.txt
#
#   2. Create .env and add your OpenAI key:
#        copy .env.example .env
#      Then open .env and set OPENAI_API_KEY=sk-...your-key...
#      NEVER paste your API key into chat or commit it to git.
#
#   3. Fetch HAPI IG tarballs (requires internet):
#        powershell -ExecutionPolicy Bypass -File docker\hapi\fetch_igs.ps1
#
# DAILY RESTART (after initial setup):
#
#   docker compose up -d fhir          # start HAPI (~90s first boot)
#   python scripts/load_synthea.py load # reload patients
#   python scripts/demo_evidence.py <patient_id>   # run demo
#
# Get a patient ID:  python -c "import json; m=json.load(open('data/synthea-config/manifest.json')); print(m['patients'][0]['patient_id'])"
# =============================================================================
requests>=2.31.0
httpx>=0.27.0
langgraph>=0.2.0
langchain-openai>=0.1.0
langsmith>=0.1.0
pydantic>=2.7.0
structlog>=24.0.0
lancedb>=0.8.0
langchain-community>=0.2.0
pypdf>=4.0.0
rich>=13.0.0
fastapi>=0.111.0
uvicorn>=0.29.0
langgraph-checkpoint-sqlite>=1.0.0
