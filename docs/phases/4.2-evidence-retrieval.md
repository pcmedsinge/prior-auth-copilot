# Phase 4.2 — Synthea data + FHIR MCP evidence retrieval

**Week**: 3 · **Status**: 🔄 In progress

## Goal

End-to-end vertical slice: given a synthetic patient and a proposed service, retrieve and rank the clinical evidence package via FHIR MCP tools.

## Scope — what ships

1. **Synthea pipeline** ✅ Issue #3 complete
   - Custom Synthea GMF module (`data/synthea-config/modules/low_back_pain.json`) — 4 evidence paths (complete, missing_conservative, short_conservative, nsaids_only).
   - HAPI FHIR v7.4.0 on port 8082 via `docker-compose.yml`; US Core + Da Vinci PAS IGs loaded (ADR-0002).
   - ~50 synthetic patients with varied evidence-completeness loaded via `make load-synthea`.
   - Tag-based idempotent re-load (`meta.tag = synthea-v1`); curated manifest at `data/synthea-config/manifest.json`.
   - Smoke test: `make smoke` verifies patient count, evidence presence, and 5 evidence-missing cases.

2. **FHIR MCP integration**
   - Reuse [`fhir-mcp-suite`](https://github.com/pcmedsinge/fhir-mcp-suite) as the MCP server.
   - Wire MCP tools into a minimal LangGraph `Evidence Gatherer` node.
   - Tools surfaced: `find_observations`, `find_conditions`, `find_procedures`, `find_medication_history`, `find_imaging_studies`, `find_documents`.

3. **Evidence ranking**
   - Deterministic first-pass: tag each retrieved resource against a hardcoded MRI-lumbar-spine policy checklist (e.g., "≥6 weeks conservative therapy", "neurological deficit", "failed PT").
   - LLM-mediated second pass: rank relevance and produce a one-line "why this matters" per resource.

4. **Demo**
   - One command: `make demo-evidence PATIENT=<id>` → prints structured evidence package to stdout + writes JSON.

## ADRs likely needed

- **ADR-0002** — FHIR server choice → ✅ Accepted (HAPI chosen).
- **ADR-0003** — How patient and service context is passed across the LangGraph state (typed TypedDict vs Pydantic model).

## Eval gates

| Metric | Target |
|---|---|
| Tool-call accuracy on 20-case golden set | ≥ 0.90 |
| Recall of "must-have" evidence resources (deterministic checklist) | 1.00 |
| Citation grounding (no fabricated resource references) | 1.00 |
| End-to-end latency (P50) | < 8s |

## LinkedIn post (Week 3)

**Voice**: IC ("Shipped:")
**Hook**: *"This week, the co-pilot can read a chart. Here's what 'reading a chart' actually means when the goal is a defensible PA evidence package."*

## Definition of Done

- ✅ Synthea pipeline: `make fhir-up && make load-synthea && make smoke` runs on a fresh checkout in <10 minutes.
- ✅ ADR-0002 accepted. ADR-0003 pending (Issue #2).
- ✅ README status badge advanced to "Phase 4.2 · Evidence retrieval".
- ⏳ FHIR MCP integration wired into Evidence Gatherer node (Issue #4).
- ⏳ LangGraph graph with Evidence Gatherer node (Issue #5).
- ⏳ Eval scorecard committed at `docs/evals/4.2-scorecard.md` (Issue #6).
- ⏳ `make demo-evidence PATIENT=<id>` runs end-to-end (Issue #7).
