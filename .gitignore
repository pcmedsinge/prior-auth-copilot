# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Node
node_modules/
dist/
build/
.next/

# Env / secrets
.env
.env.*
!.env.example
*.pem
*.key

# IDE
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Data / models / large artifacts
data/raw/
data/synthea/output/
*.ndjson
*.parquet
*.bin
*.gguf
models/

# Synthea generated output (re-created by `make load-synthea`)
out/

# Eval runner JSON outputs (contain patient IDs — regenerate locally)
evals/results/*.json

# LanceDB policy vector store (rebuilt by `make ingest-policies`)
data/policy-store/

# HAPI IG tarballs (fetched by docker/hapi/fetch_igs.sh — not committed)
docker/hapi/igs/*.tgz
docker/hapi/igs/*.tar.gz

# Logs
*.log
logs/

# Local notes (PLAN.md stays — it's the public roadmap context for this workspace only)
.scratch/

# Local-only planning notes (not part of the project)
PLAN.md

